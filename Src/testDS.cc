#include <iostream>
#include <stack>
#include <queue>
#include <map>
#include <set>
#include <vector>

// template <typename T>
// #include <stdio.h>
// #include <stdlib.h>
// #include "sys/queue.h"

const char* basket[] = {"{}", "()", "[]"};

int testDataStructure() {
    printf("Hello Balanced Parentheses inExpression World!\n");

    std::stack<int> s;
    std::queue<std::string> q;
    std::set<int> st;
    std::map<int, int> m;
    std::vector<int> v;

    s.push(20);
    return 0;
}